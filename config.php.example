<?php
// Configuration file for Breeze Time Tracker Dashboard
// Copy this file to config.php and update with your actual values

return [
    // Your Breeze PM API key - Get this from your Breeze account settings
    'breeze_api_key' => 'YOUR_BREEZE_API_KEY_HERE',
    
    // Your hourly rate for calculating earnings
    'hourly_rate' => 115,
    
    // Users to exclude from champion leaderboard (case insensitive)
    // Examples: ['admin', 'manager', 'test-user']
    'excluded_users' => ['admin'],
    
    // API timeout in seconds
    'api_timeout' => 10,
    
    // Progress level configuration
    'progress_levels' => [
        // Configure your daily earnings thresholds for the 20-level system
        // Each level represents a target daily earnings amount
        // Default: $92 per level ($0, $92, $184, $276... up to $1,840 for level 20)
        'daily_increment' => 92,
        'max_level' => 20,
        
        // Rainbow threshold - earnings amount that triggers rainbow background
        // Set to 0 to disable rainbow mode, or set higher than max level to require exceptional performance
        'rainbow_threshold' => 1500,
        
        // Monthly level scaling for monthly view
        // Monthly levels use this multiplier: daily_increment * monthly_multiplier
        // Default: 5 (assumes ~5 working days per week average)
        'monthly_multiplier' => 5,
        
        // Custom level names/descriptions (optional)
        // You can customize what each level represents
        'level_descriptions' => [
            1 => 'Getting Started',
            5 => 'Building Momentum', 
            10 => 'On Fire!',
            15 => 'Crushing It!',
            20 => 'Legendary Status',
        ]
    ]
];
?>